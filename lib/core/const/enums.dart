// enum SnackBarType { success, error, warning, info }

// enum InvoicePaidStatus { unpaid, partial, paid }

// enum SortType { asc, desc }

// enum DiscountType { percentage, amount }

// // enum OrderStatus { pending, processing, shipped, delivered, canceled }

// enum SocialLogin { facebook, google }

// enum VisitorType { person, business, guest }

// enum PaymentMethod { cash, credit, bank }

// enum Gender {
//   male,
//   female;

//   String nameArabic() {
//     switch (this) {
//       case male:
//         return "ذكر";
//       case female:
//         return 'أنثي';
//     }
//   }
// }
