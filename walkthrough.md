# Native Background Tracking Implementation

لقد تم بنجاح تطبيق ميزة التتبع في الخلفية (Native Background Tracking) لتطبيقك عبر نظامي Android و iOS باستخدام `MethodChannel`.

## أبرز التغييرات التي تمت

### 1. طبقة Flutter (Dart)
- تم استبدال الكود القديم المعتمد على `geolocator` الذي كان يستهلك البطارية ويتوقف عند إغلاق التطبيق.
- تم إضافة خدمة جديدة `NativeTrackingService` تتواصل مع الـ Native عبر قناة `com.metw_go/location_tracking`.
- تم تحديث صفحات الواجهة (مثل `splash_screen` و شاشات التواجد أونلاين/أوفلاين) لاستخدام هذه القناة.

### 2. بيئة Android (Kotlin)
- **Permissions**: تمت إضافة الأذونات المطلوبة للعمل في الخلفية وإظهار الإشعارات المستمرة (`POST_NOTIFICATIONS`).
- **Foreground Service**: تم إنشاء خدمة `LocationTrackingService` تعمل بشكل دائم في الخلفية ولا يمكن إغلاقها بسهولة من قِبل النظام، حيث تظهر إشعاراً للسائق. تقوم بطلب الموقع باستخدام أحدث تقنيات `FusedLocationProviderClient`.
- **Auto-restart**: تم برمجة `BootReceiver` لإعادة تشغيل خدمة التتبع تلقائياً في حال قام السائق بإعادة تشغيل الهاتف وكان وقتها متصلاً (Online).
- **Firebase Direct Upload**: الخدمة ترفع بيانات الإحداثيات مباشرة باستخدام `FirebaseDatabase` للـ Android Native، مما يضمن وصول البيانات بسرعة وعدم تأثرها بحالة الـ Flutter Engine.

### 3. بيئة iOS (Swift)
- **Background Location Manager**: تم تعديل `AppDelegate.swift` لإنشاء كائن من `CLLocationManager` مُخصص للعمل في الخلفية.
- **Firebase Direct Upload**: يتم تحديث موقع السائق ورفعه مباشرة إلى Firebase Realtime Database من داخل الكود المكتوب بلغة Swift.
- تم التأكد من إعدادات `Info.plist` المطلوبة لدعم التتبع المستمر.

## نصائح لاختبار الميزة
1. **أندرويد**: قم بفتح التطبيق وجعل الحالة "مستعد للاستلام" (Online). سترى إشعار التتبع في الأعلى. قم بالخروج التام من التطبيق (Swipe up / Kill). تحرك بالسيارة، ولاحظ عبر صفحة Firebase في المتصفح أن إحداثيات موقعك تتحدث.
2. **إعادة تشغيل الهاتف**: أثناء كونك "متصل"، أعد تشغيل هاتف الأندرويد. ستلاحظ أن إشعار التتبع سيظهر تلقائياً بمجرد فتح الشاشة.
3. **iOS**: عند ظهور الرسالة التي تطلب الصلاحية، تأكد من اختيار **"Allow Always"** وليس "While using the app" لضمان عملها بشكل مثالي في الخلفية.

> [!TIP]
> الكود الحالي يعتمد على كلاسات Firebase الأصلية (Native SDKs)، في حال رغبت بتعديل البيانات المرفوعة (مثل إضافة السرعة أو اتجاه السيارة) ستقوم بتعديلها من ملفات الكوتلن والسويفت الموضحة في الخطة.
